<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
<body>
  <div class="container">
    <h1>Budgetr All</h1>
    <table>
  <% for(let i = 0; i < budget.length; i++) { %>
    <tr>
        <td><%=budget[i].date%></td>
        <td><a href="/budget/<%=i%>"><%=budget[i].name %></a></td>
        <td><%=budget[i].amount%></td>
    </tr>
<% } %>
<a href="/budget/new"><button>Create New Item</button></a>
</table>
  </div>

</body>
</html>